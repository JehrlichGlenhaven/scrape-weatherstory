name: Scrape NWS Weather Story Images

on:
  schedule:
    # Run every hour from 10:00 UTC to 22:00 UTC (covers 6 AM - 6 PM EDT; 5 AM - 5 PM EST).
    # Two off-peak slots per hour: GitHub scheduled runs are best-effort and
    # minute-0 jobs are frequently delayed or dropped under load.
    - cron: '11,43 10-22 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Scrape Weather Story images
        run: python scripts/scrape_weatherstory.py

      - name: Commit and push updated images
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add assets/
          git diff --cached --quiet || git commit -m "Update NWS Weather Story images"
          git push
